# TranslatorToolBundle

## Docker install

```
# Builds image.
docker-compose build

# Runs container.
docker-compose run bundle

# Runs bundle requirements
bin/install
```

## Unit testing

```
# Runs the test classes into Tests/ dir.
bin/test
```
